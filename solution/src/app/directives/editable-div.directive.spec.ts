import { EditableDivDirective } from './editable-div.directive';

describe('EditableDivDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableDivDirective();
    expect(directive).toBeTruthy();
  });
});
